syntax = "proto3";
package protogen;
option go_package = "./protogen";

service D2 {
	rpc Move (MoveDetails) returns (Empty);
}

message MoveDetails {
	enum Direction {
		FORWARD = 0;
		BACKWARD = 1;
		LEFT = 2;
		RIGHT = 3;
	}
}

message Empty {};
